name: Research Artifact & Benchmark CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  validate-benchmarks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install evaluation dependencies
        run: |
          python -m pip install --upgrade pip
          pip install numpy scipy pandas statsmodels

      - name: Verify telemetry and ANOVA reproducibility
        run: |
          python benchmarks/reports/anova_analysis.py
